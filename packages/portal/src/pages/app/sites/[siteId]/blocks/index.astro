---
import AppLayout from '../../../../../layouts/AppLayout.astro';
import BlockTypeManager from '../../../../../components/BlockTypeManager.tsx';
import { getStore } from '../../../../../lib/datastore';
import { paths } from '@typeroll/shared';
import type { Site } from '@typeroll/shared';
import { loadSiteContext } from '../../../../../lib/access';

const ctx = await loadSiteContext(Astro);
if ('redirect' in ctx) return Astro.redirect(ctx.redirect);
const { session, site, owner_org_id: orgId } = ctx;
const siteId = site.id;

const store = getStore();
---
<AppLayout title={`Block — ${site.name}`} siteId={siteId}>
  <BlockTypeManager client:load siteId={siteId} />
</AppLayout>
