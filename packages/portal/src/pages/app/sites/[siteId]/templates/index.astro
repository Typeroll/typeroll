---
import AppLayout from '../../../../../layouts/AppLayout.astro';
import TemplateList from '../../../../../components/TemplateList.tsx';
import { loadSiteContext } from '../../../../../lib/access';

const ctx = await loadSiteContext(Astro);
if ('redirect' in ctx) return Astro.redirect(ctx.redirect);
const { session, site, versionId, owner_org_id: orgId } = ctx;
const siteId = site.id;
---
<AppLayout title={`Templates — ${site.name}`} siteId={siteId}>
  <TemplateList client:load siteId={siteId} />
</AppLayout>
